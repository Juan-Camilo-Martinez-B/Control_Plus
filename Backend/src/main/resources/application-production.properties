# ============================================
# Configuración de Producción (Railway)
# ============================================
# Este archivo se usa automáticamente cuando la variable 
# SPRING_PROFILES_ACTIVE=production está configurada

spring.application.name=backend

# Puerto: Railway proporciona la variable $PORT automáticamente
server.port=${PORT:8080}
spring.mvc.pathmatch.matching-strategy=path_pattern_parser

# ============================================
# Configuración de Correo SMTP
# ============================================
# Lee de variables de entorno, si no existen usa valores por defecto
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:martinez.b.juan.camilo@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eahw cidj jfel vowa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}
spring.mail.from=${MAIL_FROM:martinez.b.juan.camilo@gmail.com}

# ============================================
# Configuración MongoDB Atlas
# ============================================
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://juanmartinezbed:thzc7manda@controlplus.wr9wpcb.mongodb.net/ControlPlus?retryWrites=true&w=majority&appName=ControlPluss}

# ============================================
# Configuración de archivos
# ============================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

